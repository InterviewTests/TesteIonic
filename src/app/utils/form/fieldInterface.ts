export interface FieldInterface {
    type: string,
    formControlName: string,
    icon?: string,
    color?: string,
    placeholder?: string,
    required?: boolean,
    minlength?: number,
    maxlength?: number,
    autofocus?: boolean,
    active?: boolean
}
